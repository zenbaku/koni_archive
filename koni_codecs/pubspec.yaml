name: koni_codecs
description: >-
  Pure Dart compression codecs (deflate, LZMA/LZMA2 — decode and encode) and crypto primitives, usable standalone or inside the koni_archive ecosystem.
version: 0.9.0
repository: https://github.com/zenbaku/koni_archive
topics:
  - compression
  - lzma
  - deflate
  - cryptography
environment:
  sdk: ^3.7.0
resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
