name: koni_tar
description: >-
  TAR reader and writer (ustar, PAX, GNU extensions) for the koni_archive ecosystem.
version: 0.8.0
repository: https://github.com/zenbaku/koni_archive
topics:
  - archive
  - tar
environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  koni_archive_core: ^0.8.0

dev_dependencies:
  archive: ^4.0.0
  lints: ^6.0.0
  test: ^1.26.0
