name: koni_http_source
description: >-
  HTTP-range ByteSource for the koni_archive ecosystem — read entries out of a
  remote archive over HTTP Range requests, without downloading the whole file.
version: 0.6.0
repository: https://github.com/zenbaku/koni_archive
topics:
  - archive
  - http
environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  koni_archive_core: ^0.6.0
  http: ^1.2.0

dev_dependencies:
  koni_zip: ^0.6.0
  lints: ^6.0.0
  test: ^1.26.0
