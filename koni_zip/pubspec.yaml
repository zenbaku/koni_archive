name: koni_zip
description: >-
  ZIP reader and writer (incl. WinZip AES) for the koni_archive ecosystem, including CBZ comic archives.
version: 0.7.0
repository: https://github.com/zenbaku/koni_archive
topics:
  - archive
  - zip
  - compression
environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  koni_archive_core: ^0.7.0
  koni_codecs: ^0.7.0

dev_dependencies:
  archive: ^4.0.0
  lints: ^6.0.0
  test: ^1.26.0
