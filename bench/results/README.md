# Benchmark results

Committed benchmark results, one file per run, named
`<milestone>-<benchmark>-<platform>.md` and referenced from the CHANGELOG of
the package whose hot path they measure. No results yet: the first hot-path
milestone is M2 (TAR).
